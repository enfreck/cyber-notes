Library Trace - modifies the program at runtime such that every time a library function is called (ex. strlen, printf) it prints out the arguments to the screen

## ltrace <file.exe>
![](../../Images/ltrace.png)